<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cisco.library">


    <application
        android:supportsRtl="true">

        <receiver android:name="com.cisco.core.receive.NetWorkReceive">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
