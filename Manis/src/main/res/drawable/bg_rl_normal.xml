<?xml version="1.0" encoding="UTF-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充的颜色 -->
    <stroke android:color="@color/login_rl"
        android:width="1dp"/>
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:radius="28dip" />

    <solid android:color="@color/login_rl"
        android:alpha="0"/>
    <!--&lt;!&ndash; padding：Button里面的文字与Button边界的间隔 &ndash;&gt;-->
    <padding
        android:left="5dp"
        android:right="5dp"
        />
</shape>